from tkinter import *

aken = Tk()
aken.title("Pilved")
aken.configure()
aken.geometry("450x200")
aken.resizable(0,0)


tekst = Label(aken, text="mis on pilvede kõrgus? (Km)")
tekst.grid(row=0, column=1)
sisestus = Entry(aken)
sisestus.grid(row=1,column=1)

def prindi():
    print(sisestus.get())
    
    if sisestus > 6.0:
        print(sisestus)



nupp1 = Button(aken, text="OK", width=7, height=1,command=prindi)
nupp1.grid(row=5, column=2)



aken.mainloop()








aken.mainloop()
